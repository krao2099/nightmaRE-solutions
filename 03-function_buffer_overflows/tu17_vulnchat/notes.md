two buffers. One has a overflow of 10 bytes into the format string.

Can overwrite format string to allow for a increased buffer overflow. Can also allow for address leaks


print flag at 0x0804856b, no pie